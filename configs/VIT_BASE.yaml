model:
  name: vit_base
  n_class: 10

data:
  name: imagenette
  train_dir: dataset/imagenette2/train
  val_dir: dataset/imagenette2/val
  image_size: 224
  batch_size: 64
  patch_size: 16
  input_dim: 768
  hidden_dim: 3072
  n_heads: 12
  n_layers: 12
  drop_p: 0.1
  num_workers: 0
  mean: [0.485, 0.456, 0.406]
  std: [0.229, 0.224, 0.225]
  inference_dir: dataset/imagenette2/val/

train:
  epochs: 30
  lr: 3.0e-4
  momentum: 0.9
  weight_decay: 0.01
  use_amp: true
  
scheduler:
  name: multistep

save:
  checkpoint_dir: results
  best_model_name: best_vit.pth